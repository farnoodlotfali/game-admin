export enum INPUT_TYPES {
  TEXT = "text",
  CHECKBOX = "checkbox",
  NUMBER = "number",
  CUSTOM = "custom",
  SELECT = "select",
  PASSWORD = "password",
  TEXTAREA = "textarea",
  DATE = "date",
}
